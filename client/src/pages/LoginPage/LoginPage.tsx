import React from "react";
import Form from "../../components/UI/Form/Form";

const LoginPage = () => {
    return <div>
        <Form submitHandler={() => {}} buttonCaption='Sign In'/>
    </div>
}

export default LoginPage;